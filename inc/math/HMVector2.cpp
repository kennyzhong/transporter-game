#include "math/HMath.h"

namespace hmath
{
	const v2 v2::zero(0,0);
	const v2 v2::unitX(1,0);
	const v2 v2::unitY(0,1);
	const v2 v2::neg_unitX(-1,0);
	const v2 v2::neg_unitY(0,-1);
	const v2 v2::unit(1,1);
}